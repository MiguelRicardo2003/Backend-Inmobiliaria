import { check } from 'express-validator';

export const createPropiedadValidator = [
  // check('titulo').notEmpty().withMessage('El título es requerido'),
];

export const updatePropiedadValidator = [
  // check('titulo').optional(),
]; 